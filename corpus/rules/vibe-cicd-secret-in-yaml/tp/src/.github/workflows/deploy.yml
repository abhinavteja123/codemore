name: Deploy
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: "sk-proj-abcdefghijklmnopqrstuvwxyz0123456789ABCDEF"
    steps:
      - uses: actions/checkout@v4
      - name: Debug
        run: echo ${{ secrets.PROD_DB_URL }}
      - name: Call upstream
        run: curl -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" https://api.example.com
