import os                       # ← flag (never referenced)
from typing import List         # ← flag (never referenced)
import json as j                # ← flag (alias never referenced)

# `sys` IS used below.
import sys

def main():
    sys.exit(0)
